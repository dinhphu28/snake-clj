# Snake

Just a for-fun game built with functional language (Clojure).

This game uses Quil (a Clojure wrapper around Processing) for rendering.

Currently, this is just a draft simple version.

Run it with:

```sh
clj -M:run
```

